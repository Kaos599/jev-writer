node_modules/
.env
.env.*
!.env.example

# Never commit anyone's exported content or anything derived from it.
# report.json and dashboard.html both embed the full text of every post, so the
# output directory is ignored wholesale rather than by file type. An earlier
# version listed `*.jsonl` and `dashboard.html`, which missed `jev-out/report.json`
# entirely.
jev-out/
data/
out/
*.jsonl
dashboard.html
report.json

.DS_Store
